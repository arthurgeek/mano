// Fibonacci recursivo

olhaEssaFita fib(n) {
    sePá (n <= 1) toma n;
    toma fib(n - 1) + fib(n - 2);
}

// Mostra os primeiros 20 números de Fibonacci
seVira (seLiga i = 0; i < 20; i = i + 1) {
    salve fib(i);
}
