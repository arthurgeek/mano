// Fatorial recursivo

olhaEssaFita fatorial(n) {
    sePá (n <= 1) toma 1;
    toma n * fatorial(n - 1);
}

salve "5! =";
salve fatorial(5);

salve "10! =";
salve fatorial(10);

salve "12! =";
salve fatorial(12);
