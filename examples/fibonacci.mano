// Fibonacci - todos os números até 10000

seLiga atual = 0;
seLiga temp;

seVira (seLiga próximo = 1; atual < 10000; próximo = temp + próximo) {
    salve atual;
    temp = atual;
    atual = próximo;
}
